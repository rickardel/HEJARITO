@model IEnumerable<HEJARITO.Models.Activity>

@{
    ViewBag.Title = "Lista";
}

<h2>Lista</h2>

@if (User.IsInRole("Teacher")) //TM 2018-03-12 15:11
{
    <p>
        @Html.ActionLink("Skapa ny", "Create", "", new { @class = "btn btn-xs btn-success" })
    </p>
}

@if (User.Identity.IsAuthenticated) //TM 2018-03-13 13:26
{
    <table class="table table-striped table-bordered">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ActivityType.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Module.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>

            @*  //TM 2018-03-16 14:41 Ska inte visas, sparar skärmutrymme
                <th>
                    @Html.DisplayNameFor(model => model.Description)
                </th>
            *@

            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>


            @*  //TM 2018-03-16 14:41 Ska inte visas, sparar skärmutrymme
                <th>
                    @Html.DisplayNameFor(model => model.DeadlineDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.EndDate)
                </th>
                <th></th>
                *@
        </tr>

        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ActivityType.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Module.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>

            @* //TM 2018-03-16 14:41 Ska inte visas, sparar skärmutrymme
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
            *@

            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>

            @* //TM 2018-03-16 14:41 Ska inte visas, sparar skärmutrymme
        <td>
            @Html.DisplayFor(modelItem => item.DeadlineDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EndDate)
        </td>
            *@

            <td>
                @if (User.IsInRole("Teacher")) //TM 2018-03-12 15:11
        {
            @Html.ActionLink("Redigera", "Edit", new { id = item.Id }, new { @class = "btn btn-xs btn-warning" })
            @Html.ActionLink("Ta bort", "Delete", new { id = item.Id }, new { @class = "btn btn-xs btn-danger" })
}
                @Html.ActionLink("Detaljer", "Details", new { id = item.Id }, new { @class = "btn btn-xs btn-info" })
            </td>
        </tr>
                }

            </table>
        }
